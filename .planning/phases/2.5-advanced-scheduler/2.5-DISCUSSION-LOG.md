# Phase 2.5: Advanced Scheduler - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-30
**Phase:** 2.5-advanced-scheduler
**Areas discussed:** Retry & Resilience, Overlap Prevention, Missed Schedule Catch-up, Job Concurrency Policy, Multi-Schedule Per Job

---

## Retry & Resilience

| Option | Description | Selected |
|--------|-------------|----------|
| Exponential backoff | Retry at 1m, 2m, 4m, 8m up to configurable max | |
| Fixed-interval retry | Retry every N minutes until max | ✓ |
| Log + move on | Keep current behavior (no retry) | |

**User's choice:** Fixed-interval retry, 3 total attempts (initial + 2 retries), 1-minute fixed interval, escalate to error + log on final failure.

### Retry Interval

| Option | Description | Selected |
|--------|-------------|----------|
| 30 seconds | 1.5 min total window | |
| 1 minute | 3 min total window | ✓ |
| 10 seconds | Aggressive recovery | |

**User's choice:** 1 minute

### Retry Count

| Option | Description | Selected |
|--------|-------------|----------|
| 3 total (initial + 2 retries) | 3 calls to backup pipeline | ✓ |
| 4 total (initial + 3 retries) | 4 calls to backup pipeline | |

**User's choice:** 3 total

---

## Overlap Prevention

| Option | Description | Selected |
|--------|-------------|----------|
| Skip this occurrence | Drop overlap, next at next cron tick | ✓ |
| Queue and run after | Run as soon as current finishes | |
| Allow parallel runs | Run simultaneously | |
| Configurable per-job | Per-job policy | |

**User's choice:** Skip this occurrence

---

## Missed Schedule Catch-up

| Option | Description | Selected |
|--------|-------------|----------|
| Resume from now | Start scheduling from current time | ✓ |
| Catch up: run all missed | Execute for every missed tick | |
| Catch up: last missed only | Only most recent missed | |
| Configurable per-job | Per-job policy | |

**User's choice:** Resume from now

---

## Job Concurrency Policy

| Option | Description | Selected |
|--------|-------------|----------|
| Fully parallel | Each job runs independently | ✓ |
| Sequential (one at a time) | Global lock on backups | |
| Per-job sequential | Different jobs can run in parallel | |

**User's choice:** Fully parallel (different jobs run independently)

---

## Multi-Schedule Per Job

| Option | Description | Selected |
|--------|-------------|----------|
| Multiple cron expressions | List of cron exprs, same pipeline | ✓ |
| Named schedules | Named configs with options per schedule | |
| Configurable backup type | Full/incremental per schedule | |

**User's choice:** Multiple cron expressions on the job, all fire the same backup pipeline

---

## The Agent's Discretion

- Webhook / Notification on completion — deferred to future phase
- Backup Window Constraints — deferred to future phase
- Schedule Calendar Preview — not discussed
- Configurable backup type per schedule — explicitly not needed

## Deferred Ideas

- Webhook / Notification hooks on completion/failure — future phase
- Backup Window Constraints (gating conditions) — future phase
